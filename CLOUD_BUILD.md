# ساخت APK با GitHub Actions

این پروژه یک workflow آماده در `.github/workflows/build-apk.yml` دارد.
بعد از آپلود پروژه در GitHub، از تب Actions می‌توان workflow با نام `Build Ersalyar APK` را اجرا کرد.
پس از موفقیت، فایل `ersalyar-debug-apk` در بخش Artifacts قابل دریافت است.
